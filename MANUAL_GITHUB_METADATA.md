# Manual GitHub Metadata

These settings should be updated in the GitHub UI or via the GitHub API. They are not part of the regular `git` history.

## Repo Descriptions

### b2-lite
Backend service built with Python, FastAPI, PostgreSQL, SQLAlchemy, Alembic, and tests.

### vacancy_bot
Telegram bot for HH.ru vacancy search with profile onboarding, matching, and PostgreSQL persistence.

### personal-finance-analyzer1
Privacy-first Flask app for local expense analysis, CSV import, auto-categorization, and dashboards.

## Topics

### b2-lite
`python`, `fastapi`, `postgresql`, `sqlalchemy`, `alembic`, `pytest`, `rest-api`, `backend`

### vacancy_bot
`python`, `telegram-bot`, `hh-ru`, `postgresql`, `api-integration`, `job-search`, `backend`

### personal-finance-analyzer1
`python`, `flask`, `personal-finance`, `csv`, `analytics`, `dashboard`, `privacy`, `pytest`

## Suggested Pinned Repositories

1. `b2-lite`
2. `vacancy_bot`
3. `personal-finance-analyzer1`
4. `sentimentpet` only if a fourth pinned repository is needed and the current state is strong enough to show publicly
